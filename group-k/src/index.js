import React from 'react';
import ReactDOM from 'react-dom';

const app = (props) => {

  return(
    <div>
      <p>Google maps API</p>
    </div>
  )
}


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

